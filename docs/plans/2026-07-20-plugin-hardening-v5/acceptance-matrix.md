# Acceptance Matrix — item → wave → files → falsifying tests

**Status: AUTHORITATIVE for per-item acceptance** (referenced by [`approach.md`](approach.md) §10.2 — created in v4;
round 3 correctly found that v3 cited this matrix without it existing). One row per in-scope item (33 findings + 3
accepted additions + 1 ride-along) plus the two gates. `research/reference-data.json` remains the evidence for *why*
each finding is real; where its fix text conflicts with `approach.md` §3, §3 wins.

**Verification classes** (defined in `approach.md` §10.3):
**U** unit/suite (`tests/run-all.sh` layers) · **G** custom grep/lint guard in Layer 1 (needed because
`claude plugin validate` passes on bare MCP names and dead references) · **C** clean-checkout CI ·
**R** runtime proof (`claude --plugin-dir`) · **I** installed-copy proof (scratch marketplace, §8.5 channel) ·
**B** rollback rehearsal.

Lane notation: N = node-canonical, B = bash-script-canonical, I = inline-canonical (§3.1). Unqualified waves apply on
every lane.

## Gates

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| **G0** launcher | 0 | probe plugin (scratchpad, not committed) | All three candidates (node exec / shell form / bash exec) probed from a PowerShell launch; per-candidate fire/no-fire recorded in `research/`; U6 spawn-failure behavior recorded **including the node-less case (node hidden from PATH — the lane-N no-runtime experience)**; PASS granted only to a candidate meeting the §3.1.1 distributability clause (no host path, prerequisites declared); **lane-N PASS additionally requires the CR-1 condition — U6 shows the node-absent spawn failure in a user-visible form** *(v11 — round 10: this row granted PASS from distributability alone)*. If the notice is invisible, G0 records the **BLOCKED** terminal outcome: **Wave 0 pauses before its CI-enable step (§9 step 4)**, Wave 4 does not start, and a new owner CR (CR-1 §Return path) is required. Negative: the absolute-interpreter diagnostic run must NOT be recorded as a PASS; a lane-N PASS recorded without the U6 visibility evidence fails this row. | R |
| **G1** POSIX runtime | 0 | — | Exactly one terminal state recorded (POSIX_RUNTIME_VERIFIED or CI_ONLY, §3.7); release claim phrased per state — the Ubuntu-CI "suite and schema verified" claim is standing and **separate** from runtime dispatch. | — |
| **U7** compatibility floor | 0 | floor probe records in `research/`; README + migration note | Lowest candidate Claude Code version installed and exercised against all **eight** relied-upon behaviors (hooks-folder precedence, exec-form args, path substitution, structured output, `validate --strict`, explicit `shell: powershell` selection, parallel matching-hook execution, **pinned-GitHub-source-object resolution** — v9: a floor that validates hooks but cannot install the SHA-pinned source is not a floor); bounded bisection interval [~2.1.140 … 2.1.216], ≤8 probes; **execution model per §3.6**: discovery is local + authenticated with the artifact committed to `research/`; the recurring CI floor job runs only auth-free checks. Negative: a floor asserted from documentation alone, or omitting the shell/parallel/pinned-source behaviors, fails this row. | C, G |
| **P3 consistency sweep** *(v9 — round 8 defeated the v8 substring allowlists with a crafted line; exemptions are now file-scoped exact full lines)* | Phase 3, per revision | `consistency-sweep.sh` + `consistency-sweep-exemptions.txt` + all current-contract artifacts | Strict mode, required-file assertions, **exact-line exemptions** (a new stale statement can never match one; additions show in the diff and are reviewed per round), **positive invariants** (eight-behavior U7, 22-source arithmetic, occurrence-addressed model, zero-hook-errors acceptance, SHA-pinned source, installed-tree assertion, two-commit sequence, lane-qualified states), **structural state validation by exact canonical value** *(v13, parser-hardened v14 — rounds 11–13 defeated pattern checks three ways: surgical substrings, pure negations, then parser semantics; word-presence checks are retired for state fields: the complete U6 and G0 entries and the `release_identity` string must equal their canonical copies in `consistency-sweep-canonical.json` **JSON-value-exact** (formatting/whitespace explicitly not bound — semantics live in parsed values), both files read by a **duplicate-key-rejecting parser**, **exactly one U6/G0 entry required, no repeated ids**, so changing a locked contract requires updating the canonical file in the same diff — visibility is the design; `external_review.next` and the version tokens remain exact-equality; CR-1 registration as a manifest Change Records **table row**; CR-1 decision by scanning **all** State markers case/whitespace-insensitively with wrapped lines joined — exactly one, normalizing to accepted — plus the retained anchored-line/section/bold-conflict checks; honest limit, ratified by round 13 as Phase-3-adequate: dual-file contradictory edits are caught by round-record review, not the script)*, and **negative self-tests: rounds 7–8's four bypass probes, round 10's three structural mutations, and rounds 11–15's eighteen negative controls + one positive control (thirteen against `status.json` incl. the `__proto__`-key and non-string-id probes, five against CR-1 incl. the `**State:** VOID` emphasis probe; each negative must flip the result, the positive — a value-preserving U6 member reorder — must still pass; generated from the live artifacts each run, unmutated files as baseline) are fixtures the script must flag/accept or it fails itself**. *(v16 — round 15: equality made recursive order-insensitive for object members / order-preserving for arrays (was `JSON.stringify`, order-sensitive → false red); CR-1 marker scan strips Markdown emphasis (`**State:** VOID` was a false green); injection probes re-anchored on the U6 object boundary so order-insensitivity holds end-to-end.)* Exits 0 before a revision is submitted; result recorded per round in `codex-review.md`. | G |

## Wave 0 — harness

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| A1 `.gitattributes` — **SUPERSEDED by [CR-2](changes/CR-2.md) (ACCEPTED 2026-07-29); that CR is the authoritative version of this row** | 0 | `.gitattributes` | *(Original text, retained unaltered per the change-control rule:)* `git ls-files --eol -- '*.sh'` reports `w/lf` for every script; fresh clone on a `core.autocrlf=true` host checks out LF (the confirmed landmine). **CR-2 corrects two things:** (1) the `ls-files --eol` clause is **not falsifying** — it reports `w/lf` for all 14 scripts with no `.gitattributes` present at all, so acceptance is the **fresh-clone working-tree check** alone; (2) scope extends to `*.sh` **and** `tests/fixtures/**`, since Layer 3 reads tab-separated fixture data where a trailing `\r` silently breaks every comparison. | U, C |
| A2 wire orphan suite | 0 | `tests/run-all.sh` | Runner output lists Layer 5 (`codex-challenge-test.js`); totals move 115/4 → 156/4 per §10.1. Negative: `run-all.sh` fails if any layer script is missing/skipped (layer-count assertion). | U |
| A3 marketplace manifest conformance | 0 | `.claude-plugin/marketplace.json` | No `version` key (Layer 1); **top-level `description` present** — round 5 reproduced `validate --strict` failing on its absence today; both fixed **before** the CI gate is enabled. Negative: dual-manifest validation (`plugin.json` AND `marketplace.json`) passes `--strict`. | U, C |
| A4 bundle snapshot *(moved from Wave 6 — R11; arithmetic corrected v6)* | 0 | snapshot commits of **all 22 pre-reconciliation sources** — both sides of the 9 shared files (18) + the 4 sibling-only files — at `docs/plans/…/snapshots/plugin-side/` and `…/skill-side/`; content hashes recorded | After the snapshot commit a clean clone contains all **22 sources** (the 13-file figure is the reconciled *output* bundle, a different count by design); hashes recorded in the ledger header with the authoritative diff count via the pinned `git diff --no-index --numstat` command (78+222=300 at 2026-07-20; re-measured at snapshot). Negative: the superseded 296 figure appears nowhere as authoritative. | C, U |
| Hosted CI matrix *(enabled last in Wave 0 — after A3/A4/U7, so the gate is achievable by construction)* | 0 | `.github/workflows/` | `windows-latest` + `ubuntu-latest` jobs run the full suite **and dual-manifest `claude plugin validate --strict`** on floor (U7) + current versions; non-blocking latest canary present; dedicated parser-degradation jobs hide node/jq only for those cases; **no secrets in any required job (fork-PR safe, R15)**; **expected-red comparator**: the wrapper passes iff the failure set equals exactly the entries of `tests/expected-failures.txt` (the 4 F18/F19 assertions; emptied after Wave 3) — any new or missing failure fails the job, `continue-on-error` prohibited. Negative: no job depends on untracked local state (fresh checkout). | C |

## Wave 1 — island + root

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| F29 phantom agents | 1 | 4 template files under `skills/documentation/resources/` | Sweep: every "Deploy the **X** agent" instruction in templates names an agent that exists in `agents/` (fails at HEAD on 4 names). | G |
| F31 dead commands | 1 | same 4 templates | Sweep: no `/audit`, `/create-prd`, `/create-brd`, `/create-adr`; only namespaced `/deepgrade:*` commands from the SKILL.md whitelist appear. | G |
| F17 name/filename drift | 1 | 5 agent files + `readiness-scan.md`, `codebase-audit.md` (callers) | Layer 1: frontmatter `name` == filename for every `agents/*.md`; reverse sweep: every agent name referenced by a command resolves. Negative: renaming frontmatter without the 5 caller lines fails the reverse sweep (the F17 landmine). | U, G |
| F01 duplicate name | 1 | 2 agent files + 2 callers | Layer 1: `name:` values unique across `agents/` (fails at HEAD); both report pipelines reference distinct, existing agents. | U, G |

## Wave 2 — agent frontmatter (one edit per file)

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| F02 missing Bash/Write | 2 | `doc-auditor.md`, `integration-scanner.md` | Layer 1: both agents' `tools` include `Bash` and `Write`. | U |
| F03 missing Agent | 2 | `plan-scaffolder.md`, `plan-auditor.md` | Layer 1: both include `Agent`. Negative: no other agent gains `Agent` as a side effect. | U |
| F07 missing Write (13) | 2 | 13 agent files | Sweep: every agent whose body mandates a `docs/audit/` output file lists `Write`. Negative: `Edit` not added anywhere. | G |
| F21 bare MCP names | 2 | 2 agents + 3 commands | Sweep: no bare `ref_*`/`*_exa`/`perplexity_*` identifiers in `tools:`/`allowed-tools:`; the closed allowlists retain their non-MCP entries. (Custom guard required — `claude plugin validate` passes on bare names.) | G |
| F27 unreachable skills | 2 | 7 agent files | Layer 1: every agent whose body references a knowledge skill either preloads it via `skills:` frontmatter or lists `Skill` in `tools`. | U |
| F32 skill teaches bare names | 2 | `skills/mcp-research/SKILL.md` | Sweep: skill states the suffix-match availability rule and qualified `mcp__<server>__<tool>` forms; no bare-name-equality rule remains; convention text matches F21's (same-string check, or the defect reships). | G |
| Ride-along `tools:` style | 2 | 7 agent files | All `tools:` lines are comma-separated strings; no flow arrays remain. | G |

## Wave 3 — doc counts Pass A

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| F18 CHANGELOG stale | 3 | `CHANGELOG.md` | Layer 1 version cross-check green (one of the 4 expected reds clears); entries exist for 4.30.0 and 4.31.0. | U |
| F19 README counts | 3 | `README.md`, `commands/help.md` | Layer 1 version/count checks green (remaining reds clear); help.md knowledge-skills count = 6. Negative: README's "22 agents" line unchanged — it is correct (scan correction). *(Was cited as `README.md:160`; the string is now at :159 and will move again. Substance holds.)* | U |
| F20A GUIDE snapshot | 3 | `GUIDE.md` | Layer 1 (new assertions): GUIDE version = plugin.json version; counts correct; "9-phase". | U |
| F04 update-flow docs | 3 | `GUIDE.md` §10, `CONTRIBUTING.md`, `README.md` | Sweep: no "picks up changes automatically" claim; every install command uses `@deepgrade-marketplace`; live-edit flow described only for `--plugin-dir`. | G |

## Wave 4 — hooks epic (lanes N/B/I; see §3.1, §9)

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| F05 behavior ledger | 4a | surviving implementation (N: `scripts/dg-*.js`; B: `scripts/dg-*.sh`; I: consolidated handlers as tested fixtures) | Per-row tests for all 11 ledger rows, e.g. row 1: `supabase db push` denied / `supabase db diff` allowed; row 2: backslash-path migration caught; rows 5–6: inactive with `DG_STRICT_GIT` unset (default-off negative), active when set; row 7: nudge at threshold 15; row 8: tolerant read of both tracker keys. | U |
| F24 parser contract *(rewritten in v5 — owner decision: never deny on unparsed payload; raw-payload denial deleted; disposition PARTIAL)* | 4a | blocking guards + tests | **Lane-qualified states (v9 — a generic three-state matrix was impossible for lane N, whose guards run on node): lane N = node-present / node-absent; lanes B/I = jq-only / node-only / neither.** Enforcing states: the F24 reproduction payload (`git commit -m \"wip\" && git push --force`) is denied via the **parsed** `tool_input.command` field; corpus includes cross-field decoys (danger string only in `description`/`content` → allowed), quoted-text mentions (commit message naming a force-push → allowed), exemption-position cases (`--force-with-lease`/`--dry-run` honored only inside the command field), and malformed payloads (→ denied, fail-closed). **No-parser state:** guard allows the event AND emits the static JSON `systemMessage` (observed), never stderr-on-exit-0; the lane's **single** SessionStart handler behaves per §3.1.6 (v8): warning emitted when parsers absent but the handler's runtime present; **zero hook errors on healthy supported hosts** (runtime-verified locally + at the U7 floor); lane N node-absent case shows the vendor's spawn-failure notice (U6), nothing else. **CI/local boundary (v10 — round 9: CI cannot assert live vendor dispatch under the auth-free contract):** CI runs lane N's node-present state and B/I's three states; lane N's node-absent vendor-notice case is **local runtime evidence** (4c + U6 record, committed per §3.6). Disposition: release notes record F24 **PARTIAL — enforced only with node/jq**. Informational hooks fail open in every state. | U, C, R |
| F22 deny-vs-ask | 4a | guards + `layer2` | `git reset --hard` → `permissionDecision:"ask"` JSON, exit 0; force-push/DB-deploy remain deny with "BLOCKED" wording. The old `exit 2` + "WARNING" assertion is deleted (it enshrined the defect). | U |
| F25 force-with-lease | 4a | guards + tests | `git push --force-with-lease` allowed; `--force` and bare `-f` denied. | U |
| F26 invisible output | 4a impl / 4c verify | Stop, PreCompact, SessionStart handlers | Unit: all exit-0 outputs are JSON (`systemMessage` / documented context form) — settles U4; runtime: message observed live per event — settles U5. **Terminal disposition locked (v5):** if PreCompact cannot surface a message, the fallback (compact-resume message via the SessionStart `compact`-source path) is implemented and verified; if that also fails empirically, F26's PreCompact half is recorded PARTIAL in the release notes — no silent scope escape. | U, R, G |
| Layer 2 rewrite | 4a | `tests/layer2-hook-suite.sh` + `run-hook-corpus.js` + `layer2-ledger-rows.js` *(citation corrected 2026-07-30: the row named `layer2-hook-simulation.sh`, which was DELETED at 4b along with the inline hooks it drove. The clause is satisfied by removing its subject, not by rewriting the file — both the handlers and the positional-index technique are gone.)* | No positional-index extraction; every guard case runs in the **lane-qualified §3.1.6 state set** (lane N: node-present only in CI; B/I: jq-only/node-only/neither); baseline contract updated in §10.1 terms. | U |
| F06 orphaned scripts | 4b | `hooks/hooks.json` + `scripts/` + `layer1` §15 (hook wiring) + README "Safety Hooks (N)" + GUIDE subtitle *(citation corrected 2026-07-30: `layer1:111,:120-121,:265` now point at a comment, a blank line and an unrelated version check — the file has grown past 1000 lines. Cited by SECTION rather than line, since a line number in a live file is a citation designed to go stale.)* | Lanes N/B: every file in `scripts/` is referenced by the hook config (reverse sweep) and the SubagentStop entry exists (count 8). Lane I: `scripts/` is absent. Either way: no unwired implementation ships. | U, R, I |
| F23 inline architecture | 4b (N/B) | `plugin.json`, `hooks/hooks.json` | Lanes N/B: Layer 1 asserts `plugin.json` has **no** `hooks` key AND `hooks/hooks.json` exists and parses — never both (the silent-ignore constraint). Lane I: **formally NOT MET** — release notes must record it (grep gate), quoting-lint test ships as mitigation. | U, G |
| SubagentStop (§3.1.5) | 4b | `hooks/hooks.json` + docs + `layer1` §15 | Entry present on every lane; handler count 8 in README/GUIDE/layer1; observed firing in 4c. | U, R |
| Runtime proof | 4c | `tests/layer7-runtime-proof.sh` (new) | Every shipped event **and** matcher observed firing (1) via `claude --plugin-dir` and (2) from an installed copy on the §8.5 scratch channel — catches cache-copy differences (path resolution, executable bits). Runs on the surviving lane, **including lane I**. **Adds the node-less installed-copy case** (node hidden): under lane N, guards **and** the SessionStart handler are unspawnable — the observed signal is the **vendor hook-error notice only** (U6 records its exact form; no friendly warning exists on this host — the §3.1.6 lane-qualified contract, owner acceptance recorded in §11/CR). Under lanes B/I this case instead shows the bash handler's systemMessage warning. Independent review (§10.4) runs **after** this step, with its artifacts pinned. | R, I |

## Wave 5 — command hygiene

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| F09 `$1` substitution | 5 | `quick-cleanup.md` | Sweep: no `$1` inside bash blocks; folder arg uses `$0`/prose placeholder. Manual cases: two-argument invocation maps folder→folder, **and the zero-argument case degrades safely** — an unsubstituted `$0` must not fall through to the shell name (round 5's hole: negative test required). | G |
| F10 `${PROJECT_ROOT}` | 5 | `plan-export.md` | Sweep: no `${PROJECT_ROOT}`; `${CLAUDE_PROJECT_DIR}` used; no `$1`. | G |
| F11 dead command names | 5 | `readiness-generate.md` | Sweep: no `/ai-readiness-*` strings; `argument-hint` present. | G, U |
| F13 wrong dir guard | 5 | `plan-status.md` | Guard tests `docs/plans`; no-arg mode lists an existing plan (behavioral smoke against this plan folder). | G, U |
| F14 model invocability | 5 | 3 command files | `disable-model-invocation: true` on `codebase-gates`, `plan-export`, `readiness-generate`; **not** on `plan` (negative — §3.8 discovery-path decision). | U |
| F15 host-tool portability | 5 | `plan-export.md`, `plan-status.md`, `troubleshoot.md`, `quick-cleanup.md`, **`readiness-generate.md`** *(added v6 — round 5 found `tree -d -L 2` at `:61` live and outside acceptance despite research prescribing its replacement)* | Sweep: `zip` usage carries a `Compress-Archive` fallback; no bare `python3` one-liner without fallback; no bare `/tmp` staging; **no `tree` usage** (negative grep — unavailable in stock Git Bash). | G |
| F08 gate-generator targets | 5 | `agents/gate-generator.md` | Sweep: generation target is the `hooks` key of `.claude/settings.json` (merge); no `.claude/hooks/hooks.json` reference; PowerShell variant emitted for Windows hosts. | G |
| F28 phantom auto-invoke | 5 | 4 skill descriptions + orchestrating commands | Sweep: no "Auto-invoked" phrasing; descriptions carry concrete trigger terms; each knowledge skill is explicitly invoked or preloaded by at least one orchestrator (reverse-reference sweep). | G |
| F30 duplicate dispatcher | 5 | delete `commands/doc.md`; `help.md`; README/GUIDE counts | `commands/doc.md` absent; skill carries the `${CLAUDE_SKILL_DIR}` dispatch + plan-awareness block; no dangling `/deepgrade:doc` references (sweep); command count assertions updated. | U, G |

## Wave 6 — de-duplication (§3.3)

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| Reconciliation ledger *(inputs snapshotted in Wave 0 — see A4; segmenter corrected v7; mapping model corrected v8)* | 6 | `reconciliation-ledger.md` + generator/checker scripts | A **generated source inventory** using the explicit **blank-line segmenter** (plan.md: 304 blocks / max 22 lines), **occurrence-addressed** on both sides with explicit cardinality types (`1:1 keep`, `N:1 merge/dedupe`, `1:N split`, grouped transform, drop, generated — round 7 reproduced 4 duplicate occurrences of one segment in plan.md, which substring presence cannot distinguish). **Checker enforces exact consumption:** every input occurrence and every output span belongs to exactly one mapping; rejects gaps, overlaps, unclaimed spans, transforms without complete output declarations. **Review reads the drop list and the complete transform table.** | U |
| Renderer + `dist/` | 6 | `scripts/build-standalone-skill.sh`, `dist/troubleshooting-skill/` | Clean clone → run renderer → byte-identical to committed `dist/` (determinism). | U, C |
| **No-unledgered-loss gate** | 6 | `tests/layer6-drift-check.sh` | Diff `dist/` against **both Wave 0 snapshots**: every source hunk that neither survives verbatim nor appears in the ledger with a disposition fails the gate. Semantic checks (required sections, schema fields, phase-reference consistency) also pass — necessary but not sufficient without the loss check. | U, C |
| F33 schema divergence | 6 | technique sources + `kb-schema.md` | KB field sets unified per ledger (Plan-field disposition explicit); guardrail-miss token format single across both products (grep both). | U, G |
| F16 unresolvable paths | 6 | 5 files with bare `docs/planning-techniques/` refs | Sweep: every bundled-doc reference in `commands/`/`agents/` is `${CLAUDE_PLUGIN_ROOT}`-anchored; reference resolves from an installed copy. | G, I |

## Wave 7 — command→skill (§9.1 activation contract)

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| F12 plan.md split | 7 | `skills/plan/SKILL.md` + phase files + completeness manifest; delete `commands/plan.md` | `SKILL.md` < 500 lines; references one level deep; `${CLAUDE_SKILL_DIR}` anchors; forward slashes (sweep). **Staging proof (expanded v5):** in a scratch copy with the command removed — fresh start reaches Phase 1; fixture resume per phase (**all nine boundaries** load their phase file); **forced compaction** mid-plan with later-phase instructions still reachable; **completeness manifest built from a machine-generated source inventory** (continuous, non-overlapping hashed ranges over the 1,528-line command; checker rejects gaps, overlaps, duplicate mappings, destination mismatches, and transforms without disposition — a hand-authored mapping or a router that drops Phase 8 fails this row); invocation from an **installed copy**. Activation one commit; recovery = single revert + `/reload-plugins` (rehearsed). | U, G, R, I, B |

## Wave 8 — release

| Item | Wave | Files | Acceptance | Class |
|------|------|-------|-----------|:-----:|
| R7 version bump | 8 | `plugin.json`, `CHANGELOG.md` | Version 5.0.0; CHANGELOG entry exists (Layer 1 gates both). | U |
| Migration note | 8 | release notes / README | Carries: the four-command update sequence, the surviving lane's prerequisite + first-use check description, the **F24 PARTIAL disposition**, the **compatibility floor (U7)**, the §8.3 corrected disable procedure (uninstall **then** `/reload-plugins`), temp-file disposal, and — lane I only — the F23 NOT-MET record; G1 state reflected per §3.7 phrasing. | G |
| Final schema gate | 8 | release checklist | Dual-manifest `claude plugin validate --strict` passes at the release commit on floor and current versions (CI re-run) — the vendor-schema gate round 4 found absent. | C, U |
| **Publication gate** *(v9 — full-SHA source object, two-commit sequence, executable identity assertion; rounds 7–8: `source: "./"` was mutable, a ref-only pin is movable, and no listing command exposes the source SHA)* | 8 | `marketplace.json` (source → `{"source":"github","repo":"krwhynot/deepgrade","ref":"v5.0.0","sha":"<full>"}`); authoritative branch + tag; §8.5 profile kit | **Commit 1:** release commit + tag `v5.0.0`. **Commit 2:** catalog commit pinning the source object to the tag's full SHA. Push both. **Flow A (credentialed existing-user profile, end-to-end):** `/plugin marketplace update` → `/plugin update` → `/reload-plugins` → `claude plugin list --json` asserts version + marketplace-qualified id → guarded-command smoke **in the same profile**. **Flow B (fresh `CLAUDE_CONFIG_DIR`):** `marketplace add krwhynot/deepgrade` → install (auth-free) → `plugin list --json` (version, id) + `marketplace list --json` (repo) assertions → authenticate **that profile** → smoke on **its** copy. **Identity: normalized installed-tree comparison** — archive of the **catalog's authoritative `sha`** (with `ref` separately asserted to resolve to it, §3.6) vs `installPath`, cache metadata (`.in_use` etc.) excluded. Negative: the gate fails if any step is simulated. | I, B |
| Rollback rehearsal | 8 | scratch channel (§8.5) | 0.0.1→0.0.2 forward-rollback executed; `dist/` restore from the **Wave 0** snapshots executed; **uninstall + `/reload-plugins` → guarded command no longer denied** (hook-silence check). | B, I |
| §9.2 doc conformance | 4a/4b/3 | per §9.2 table | Each row's claim is true of the shipped lane at release (grep per row). | G |
