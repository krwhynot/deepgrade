# Expected test failures — the ONLY failures CI may tolerate.
#
# THIS FILE IS INTENTIONALLY EMPTY. Zero failures are tolerated.
#
# Emptied: 2026-07-29 by PHV5-030 (Wave 3), as the plan requires.
# It previously held four entries — findings F18 (stale CHANGELOG) and F19 (wrong
# README counts) — which were expected to be red from the Wave 0 baseline until
# Wave 3 fixed them. They are fixed; the suite is plainly green.
#
# CONTRACT (approach.md §10.3): the CI wrapper parses the suite's failure list and
# passes IFF that list equals exactly the entries below — which is now the empty set.
# Any failure at all fails the job. `continue-on-error` is prohibited.
#
# DO NOT add entries here to make a red build pass. A genuine defect belongs in a
# fix, not in this file; recording one here enshrines it as correct behaviour, which
# is the R2 anti-pattern this plan exists to remove (the pre-existing suite asserted
# two defects as correct, and that is precisely what made it untrustworthy).
#
# Suite totals when this file was emptied: 167 passed / 0 failed
#   Layer 1 Config/Wiring    95 passed / 0 failed   (86 + 9 new F20A/F04 assertions)
#   Layer 2 Hook Simulation  15 passed / 0 failed
#   Layer 3 Fixture Lint      9 passed / 0 failed
#   Layer 4 Behavioral Smoke  7 passed / 0 failed
#   Layer 5 Codex Challenge  41 passed / 0 failed
#
# Format: one exact failure message per line, '#' comments and blank lines ignored.
